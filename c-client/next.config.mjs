/** @type {import('next').NextConfig} */
const nextConfig = {
    experimental: {},
    output: 'standalone',
    typescript: {
        ignoreBuildErrors: false,
    },
  };
  
  export default nextConfig;